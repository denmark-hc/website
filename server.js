/* Proxy server removed per user request.
   The site is intended to be static and only include the sponsor page.
   If you need a proxy later, restore from history or re-add a server implementation.
*/
console.log('server.js disabled — proxy removed');
